-- PostgreSQL init script (runs on first container start only)
-- Flyway handles all schema migrations. This file ensures
-- the database and user exist with correct permissions.

-- No-op: docker-compose POSTGRES_DB/POSTGRES_USER env vars
-- handle database and user creation automatically.
