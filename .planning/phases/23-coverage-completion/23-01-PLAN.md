---
phase: 23-coverage-completion
plan: 01
type: execute
---

<objective>
Broaden @DataJpaTest coverage across the repositories that still lack one (Track A2).

Purpose: The progress log notes that AbstractDataJpaTestSupport is wired and that User, Portfolio, BudgetTransaction, Bill, Tag, and Debt repositories have suites — every other repository is unprotected and Phase 25 will start refactoring services that read through them. Closing this gap before that refactor keeps regressions cheap.

Output: A new @DataJpaTest class for every Spring Data repository that does not already have one, each gated on Docker availability and exercising at least the non-trivial query methods.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@tasks/ROADMAP.md
@backend/pom.xml

**Tech stack available:** JUnit 5, Mockito 5, Testcontainers PostgreSQL, AbstractDataJpaTestSupport (existing helper), AssertJ.
**Established patterns:** "AbstractDataJpaTestSupport runs Postgres via Testcontainers + dynamic datasource registration; concrete suites are gated on Docker availability." Tests must seed parent `User` rows so FK-checked inserts work on CI.
**Constraining decisions:**
- Flyway-only schema; tests must run after migrations apply.
- Coverage gate stays at 60% instruction / 45% branch; this plan moves coverage upward.
**Issues being addressed:** None as a regression — this is preventative.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit which repositories lack a @DataJpaTest</name>
  <files>.planning/phases/23-coverage-completion/23-01-AUDIT.md</files>
  <action>List every Spring Data repository under backend/src/main/java/com/fintrack/ (find by suffix `Repository.java`). Cross-reference against backend/src/test/java/com/fintrack/ for matching `*RepositoryTest.java`. Produce a markdown table with columns "Repository", "Source path", "Has @DataJpaTest", "Notable query methods worth testing". Save to .planning/phases/23-coverage-completion/23-01-AUDIT.md. Do not write any source code in this task — only the audit. Use Grep, not shell `find`.</action>
  <verify>The audit file exists and lists at least every repository found by `Grep "extends JpaRepository" backend/src/main/java`. Counts of "missing" rows are non-zero (otherwise the plan is unnecessary — escalate).</verify>
  <done>Audit committed; the missing list drives Task 2 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Add @DataJpaTest suites for the missing repositories</name>
  <files>backend/src/test/java/com/fintrack/{feature}/{Entity}RepositoryTest.java (one per missing repo)</files>
  <action>For each "missing" row from Task 1, create a `{Entity}RepositoryTest` extending `AbstractDataJpaTestSupport`. Cover: (1) round-trip persist + read by id; (2) every custom `@Query` or method-name-derived query that has business meaning (not raw findAll); (3) at least one negative case where the query must return empty. Seed the parent `User` row via the existing helper before inserting child entities. Use AssertJ. Mirror the style already established in `backend/src/test/java/com/fintrack/portfolio/PortfolioRepositoryTest.java` (or whichever existing suite is closest in shape). Do NOT write tests against repositories that only inherit `JpaRepository` defaults — flag them in the SUMMARY as "no custom queries, skipped intentionally". Avoid: full Spring Boot context (this is a slice test, not `@SpringBootTest`); Mockito mocks (this is integration, not unit).</action>
  <verify>`./mvnw -pl backend test -Dtest=*RepositoryTest` passes locally with Docker available; on Docker-less hosts the suites report Skipped (not Failed) thanks to the existing `@EnabledIf` gate. JaCoCo report after `./mvnw -pl backend verify` shows instruction coverage strictly higher than the pre-task baseline.</verify>
  <done>Every repository identified in Task 1 either has a new `*RepositoryTest` covering its custom queries, or is explicitly listed in the SUMMARY as "skipped intentionally — no custom queries". `./mvnw -pl backend verify` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Update progress log and close the A2 ticket in tasks/ROADMAP.md</name>
  <files>tasks/ROADMAP.md, .planning/STATE.md</files>
  <action>Append a one-line entry under "Progress log" in tasks/ROADMAP.md dated today: "A2 complete: @DataJpaTest coverage extended to N additional repositories; backend instruction coverage now X.X%, branch X.X%." Strikethrough the A2 row in the Track A table. In .planning/STATE.md update the Last activity line and note the A2 completion under "Recent decisions". Do not change anything else in either file.</action>
  <verify>`git diff tasks/ROADMAP.md` shows only the strikethrough on A2 and the new progress-log line; `git diff .planning/STATE.md` shows only the activity update.</verify>
  <done>Both files updated, no unrelated edits.</done>
</task>

</tasks>

<verification>
- [ ] `./mvnw -pl backend verify` is green with Docker available.
- [ ] On a Docker-less host, the new tests report as Skipped, not Failed.
- [ ] JaCoCo instruction and branch coverage are strictly higher than baseline.
- [ ] No production code under `backend/src/main/java` was modified by this plan.
</verification>

<success_criteria>
- All tasks committed atomically.
- Every repository surveyed in Task 1 either has a new @DataJpaTest or is listed as intentionally skipped.
- Coverage moves up (no regression).
- Progress log + STATE.md reflect the completion.
</success_criteria>

<output>
After completion, create `.planning/phases/23-coverage-completion/23-01-SUMMARY.md`:

# Phase 23 Plan 01: Repository slice coverage

**[One-liner — e.g. "Added N @DataJpaTest suites; backend coverage at X.X% instruction / X.X% branch"]**

## Accomplishments
- Audit produced (link to AUDIT.md)
- New `*RepositoryTest` files (count + names)
- Repositories intentionally skipped (count + reason)

## Files Created/Modified
- (List)

## Decisions Made
- (e.g. choice to skip a no-custom-query repo)

## Issues Encountered
- (None / list)

## Next Step
Ready for `23-02-PLAN.md` (PIT mutation testing).
</output>
