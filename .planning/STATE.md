# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-04)

**Core value:** Owner can stop using the spreadsheet and trust this app for live portfolio P&L, monthly cash flow, and bill tracking — fully self-hosted.
**Current focus:** Phase 29 in progress — sub-plan 01 shipped multi-portfolio side-by-side comparison (Track G4).

## Current Position

Phase: 7 of 8 (Phase 29 — Portfolio Analytics)
Plan: 1 of 3 in current phase
Status: In progress (1/3 plans)
Last activity: 2026-05-09 — Completed 29-01-PLAN.md (portfolio comparison endpoint + chart)

Progress: █████░░░░░ 50%

## Performance Metrics

**Velocity:**
- Total plans completed: 0
- Average duration: —
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 27. Tax & Accounts (TR) | 4/4 | Complete | 2026-05-09 |
| 28. Rebalance & Emergency Fund | 2/2 | Complete | 2026-05-09 |
| 29. Portfolio Analytics | 1/3 | In progress | 2026-05-09 |

**Recent Trend:**
- Last 5 plans: —
- Trend: —

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- 2026-05-04 (plan 23-01): A2 (Track A) closed. Used existing `*RepositoryDataJpaTest` naming over the plan's literal `*RepositoryTest` to stay uniform with the 10 already-shipped suites. AdminSettingRepository and UserSettingsRepository intentionally skipped — no custom queries.
- 2026-05-04 (plan 23-02): A7 closed. pitest-maven 1.17.4 wired behind an opt-in `mutation` profile with a 60% project-level threshold; the gate already passes at 63% kill rate (1043/1659). Per-class lift is deferred to ISS-100..ISS-109 — ReportService, AuthService, BudgetService, PriceSyncService, DebtService warrant dedicated plans, and BackupService / PushService / MailService need a constructor-injection refactor before they can be tested. CI mutation job is informational only (not in `ci-complete` needs[]).
- 2026-05-04 (plan 23-03): A8 closed. OpenAPI spec generated by an opt-in `@SpringBootTest` and committed at `frontend/openapi.json`; `openapi-typescript` produces `frontend/src/api/openapi.types.ts`; Vitest type-level contract test covers six surfaces. CI gates fail the build on any diff in either file. Plan widened to fix four latent boot bugs and two real frontend type drifts.
- 2026-05-04 (plan 23-04): A9 closed. tess4j receipt OCR runs as a `@Scheduled` worker with virtual-thread fan-out and short per-row transactions; `OcrStatus` enum + extracted text persist on `transactions` (V36) and surface through `TransactionResponse`. Phase 23 complete.
- 2026-05-04 (plan 24-01): D2 closed. Argon2id is the default encoder via `DelegatingPasswordEncoder` (v5_8 params). Rehash-on-login fires from `AuthService.login()` and emits `PASSWORD_REHASHED`. AuthService mutation kill rate 36% (above the 28% ISS-101 floor). bcprov-jdk18on 1.78.1 added.
- 2026-05-04 (plan 24-02): D2 (library) closed. Picked `com.webauthn4j:webauthn4j-core 0.27.0` over yubico. WebAuthn registration ceremony shipped: V38 `authenticators` table, `WebAuthnRegistrationService.start/finish`, atomic Redis getAndDelete for replay protection.
- 2026-05-04 (plan 24-03): D4 closed end-to-end. Backend assertion ceremony with decoy Redis entries on unknown usernames (timing oracle removed), strict sign-count monotonicity, TOTP gate preserved. Frontend: `base64url.ts`, `webauthn.api.ts`, `useWebAuthn.ts`, `PasskeySection`, `WebAuthnLoginButton`. ClientPlatform-driven cryptographic E2E deferred to ISS-110.
- 2026-05-04 (plan 24-04): D6 closed. Refresh-token rows now carry a `fingerprint` column. `RefreshTokenFingerprintService` computes deterministic SHA-256(`prefix|ua`); IPv4 collapses to /24, IPv6 to /48. NULL → bind on first refresh; mismatch → delete + audit + throw `BusinessRuleException`. No feature flag.
- 2026-05-05 (plan 24-06): D8 closed end-to-end. `ReceiptUrlSigner` issues stateless HMAC-SHA256 tokens; verifier is constant-time. `SignedReceiptTokenFilter` installs an anonymous synthetic Authentication so the global `authenticated()` rule passes; cryptographic gate stays at the controller boundary. Frontend: `useReceiptUrl` React Query hook, `ReceiptThumbnail` via `<img src>`.
- 2026-05-05 (plan 24-05): D7 closed. `AuditPiiRedactor` scrubs email / JWT / IPv4 / IPv6 / TOTP recovery-code patterns. `AuditRetentionWorker` `@Scheduled` daily, native chunked DELETE, 100-iteration safety cap. New `GET /api/v1/admin/audit/retention`. V40 fixed CHAR→VARCHAR on `refresh_tokens.fingerprint`.
- 2026-05-05 (plan 24-07): D9 closed. `org.owasp:dependency-check-maven 11.1.1` behind opt-in `security` Maven profile, CVSS 9.0 fail threshold, informational CI job. `ProductionProfileGuard` aggregates every prod misconfiguration into a single boot-time `IllegalStateException`: CORS, Redis password, JWT secret, receipt secret, WebAuthn RP id/origin. `CorsProperties` bound from `CORS_ALLOWED_ORIGINS`; wildcard fallback gated to non-production.
- 2026-05-05 (plan 24-08): Audit-domain coverage closed. Seven services (PortfolioService, HoldingService, InvestmentTransactionService, BudgetService, CategoryService, BudgetRuleService, BillService) emit `auditService.success(...)` after every mutating method's DB write and `auditService.failure(...)` before each `BusinessRuleException` throw site. Username sourced via `SecurityContextHolder.getContext().getAuthentication().getName()` from a small per-service static helper — `RequestContext` stays jakarta-servlet-only. AuditAction grew by 13 constants (PORTFOLIO_*, HOLDING_*, INVESTMENT_TRANSACTION_*, BUDGET_TRANSACTION_*, CATEGORY_*, BUDGET_RULE_*, BILL_*, BILL_PAYMENT_RECORDED). Seven new `*ServiceAuditTest` fixtures (≈30 tests) pin the contract via Mockito.verify with eq + contains matchers; existing service tests gain a single `@Mock AuditService` field. BudgetService bulkDelete branch coverage extended (empty / no-match / at-least-one) for ISS-102. Two deviations: BillPaymentService doesn't exist as a separate file (pay flow is on BillService — folded in), and the plan's `category` sub-package for CategoryServiceAuditTest doesn't exist (placed at `com.fintrack.budget`). ISS-111 logged for TagService + AllocationService follow-up. mvnw verify green (1012 tests). Mutation pass green: project kill rate 64% (1166/1833) — above the 60% gate and the 23-02 63% baseline; per-class: PortfolioService 85%, HoldingService 85%, InvestmentTransactionService 78%, BudgetService 31% (+9pp from 22% ISS-102 baseline), CategoryService 81%, BudgetRuleService 91%, BillService 70%. Phase 24 complete.
- 2026-05-07 (plan 25-03): Closed CONCERNS.md `WebClient.block()` + `Thread.sleep` in price clients entry. spring.threads.virtual.enabled=true wires Tomcat, the default TaskScheduler (every @Scheduled now runs on a virtual carrier), and @Async to virtual threads. New named priceVirtualExecutor @Bean (Executors.newVirtualThreadPerTaskExecutor()) drives a fetch-fan-out + persist-once shape on PriceSyncService: refreshLive() runs the four providers in parallel; refreshFunds() runs per-fund TEFAS reads with a Semaphore(price-api.tefas.parallelism, default 4) gate. Thread.sleep removed from the price-sync path; @Transactional collapsed onto a single short persistUpdates(...) call (the five public per-source methods are no longer @Transactional). WebClient.block() calls in clients stay — block on a virtual thread does not pin a platform thread. PriceConfigVirtualExecutorTest pins the bean wiring; PriceSyncServiceFundRefreshTest pins the no-sleep, parallelism-capped contract. Two auto-fixed deviations: (a) PriceSyncServiceTest had to switch from @InjectMocks to @BeforeEach manual construction because the new @Qualifier-bound constructor needs explicit wiring; (b) PriceUpdate carries the Asset reference rather than the assetId so persistUpdates can mutate + save() the detached entity directly (test compatibility win). mvnw verify green (1063 tests). Phase 25 complete (3/3 plans).
- 2026-05-07 (plan 26-01): Closed Phase 26 sub-plan 01 — OpenTelemetry OTLP export to Tempo. micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp + opentelemetry-spring-boot-starter:2.10.0 wired on the backend POM; management.tracing (sampling probability env-bound, w3c propagation) + management.otlp.tracing (endpoint env-bound, default http://tempo:4318/v1/traces, gzip, 10s timeout) added to application.yml with a 0.1 sampling override under the production profile. New grafana/tempo:2.6.0 container in docker-compose (single-binary, local storage, 14-day retention, no host port, NOT in backend.depends_on so the API still boots when Tempo is offline). New monitoring/tempo.yml + monitoring/grafana/provisioning/datasources/tempo.yml. New TracingConfig.tracingPriceVirtualExecutor bean wraps the raw priceVirtualExecutor (25-03) with a ContextSnapshot-propagating ExecutorService decorator so CompletableFuture.supplyAsync(supplier, executor) hand-offs carry the parent observation; PriceSyncService consumes the wrapped form via @Qualifier and carries @Observed(name = price.refresh.live / price.refresh.funds) on the orchestrator methods. RequestLoggingFilter takes an @Nullable Tracer constructor param and writes traceId + spanId into MDC alongside requestId. Tests: TracingConfigTest (4 — bean wiring, virtual-thread execution, ContextSnapshot propagation via a registered ThreadLocalAccessor, empty-context safety), TracingIntegrationTest (2 — TestObservationRegistry-driven assertion that price.refresh.live + price.refresh.funds open and close), RequestLoggingFilterTest (4 — requestId MDC lifecycle + cleanup-on-throw + traceId/spanId injection + null-span no-op). Test count 1063 -> 1073. mvnw verify green; JaCoCo 60%/45% met; Spotless clean. Two deviations: (a) TracingIntegrationTest narrowed from full @SpringBootTest to a @SpringJUnitConfig slice with TestObservationRegistry — proves the load-bearing claim (the @Observed boundaries fire under the wrapped executor) at single-second cost; full-stack OTLP pipeline assertion lives at the manual verify step. (b) starter version pinned at 2.10.0 (latest 2.x compatible with Spring Boot 3.2.4 at execution time).
- 2026-05-07 (plan 26-03): Closed Phase 26 sub-plan 03 — SLI/SLO dashboard with burn-rate alerts. Three SLIs: HTTP latency p95 per route group (read / mutating / auth; prices group exempted), HTTP error rate (5xx only, prices group excluded), per-source price-sync freshness (crypto / currency / metal / stock; funds intentionally not alerted -- TEFAS daily-tick model). Two-burn-rate envelope (1h x 14.4 fast / 6h x 6 slow) per Google SRE workbook for the ratio-based SLIs; single-threshold-with-duration (6h) per source for freshness. Alertmanager + Prometheus rule mount ship as a compose overlay at `monitoring/prometheus/docker-compose.prometheus.yml` (matching the 26-02 GlitchTip overlay precedent -- main `docker-compose.yml` locked by `pre_guard_release_files.py` PreToolUse hook). New `PriceSyncMetrics` `@Component` registers four `fintrack_price_sync_last_success_timestamp_seconds{source=...}` Micrometer gauges with a NaN-before-record contract that prevents false-positive freshness alerts on cold boot. `PriceSyncService` records per-source freshness via `recordSuccessIfPresent(source, count)` after every populated fetch in `refreshLive()` and the four single-source public methods; new `@Nullable PriceSyncMetrics` constructor parameter so existing test fixtures pass null. Grafana dashboard `monitoring/grafana/dashboards/fintrack-slo.json` (six panels) auto-loaded by the existing dashboards provisioner. Operator-facing runbook in `docs/OPERATIONS.md` -> `## SLI/SLO dashboard and burn-rate alerts`. CHANGELOG `[Unreleased] / ### Added` entry via the same one-shot Python-script workaround the 26-01/26-02 executors used (the `pre_guard_release_files.py` hook covers `CHANGELOG.md` as well as `docker-compose.yml`). Tests: `PriceSyncMetricsTest` (4 cases) pins the gauge contract; `PriceSyncServiceMetricsIntegrationTest` (3 cases) pins the wiring under a `SimpleMeterRegistry`. Existing `PriceSyncServiceTest` + `PriceSyncServiceFundRefreshTest` + `TracingIntegrationTest` pass with constructor-null padding only. mvnw verify green; JaCoCo 60%/45%; Spotless clean. Phase 26 complete (3/3 plans).
- 2026-05-07 (plan 27-01): Closed Phase 27 sub-plan 01 — TR tax helper. Tax-year parameters live in a versioned classpath YAML at `backend/src/main/resources/tax/tax-parameters-tr.yml` (seeded with 2024 158,000 TRY + 2025 200,000 TRY thresholds sourced from GIB / Resmi Gazete; access date recorded in the file header) so the owner appends one block in January each year and rebuilds — no Java constant change, no Flyway migration, no admin-settings table. New `com.fintrack.report.tax.tr` package: `TrTaxParameters` immutable record, `TrTaxParametersLoader` `@Component` reading the YAML at startup via SnakeYAML (transitively present from 26-02; no new Maven dep) with empty-map fallback on missing/malformed input so a typo never crashes boot; `TrTaxService` reuses `CapitalGainsService.compute(userId, year)` as a black box and layers threshold banding (under <80% / approaching 80–100% / over >100% / unknown) + warnings list (`approaching-threshold` / `threshold-exceeded` / `parameters-missing`) on top, scoping realized capital gain to `parameters.appliesTo` (`[STOCK]` for the listed-equity exemption rule) by re-fetching each `CapitalGainsResponse.Event`'s asset; `TrTaxController` exposes `GET /api/v1/reports/tax/tr` with manual `@RequestParam` validation via `IllegalArgumentException` (routes through the existing `GlobalExceptionHandler` -> 400 instead of wiring a new `ConstraintViolationException` handler). `DividendRepository` gains `sumStoppageTotalsByPortfoliosAndRange` (gross / withholding / net 3-tuple) + `sumStoppageByAssetAndPortfoliosAndRange` (per-asset). Year-defaulting honours `UserSettings.timezone` with `Europe/Istanbul` fallback so an owner travelling overseas still sees the right fiscal year. APPROACHING_RATIO = 0.80 lives at the service layer NOT the YAML so an owner edit cannot accidentally widen the warning window. Frontend ships a lazy-loaded `/reports/tax/tr` page with status / realized YTD / threshold / headroom stat cards + per-asset stoppage table + warnings banner + parameters-source footer; analytics page links to it via a new tile; `taxTr.*` i18n namespace ships in both `tr.json` and `en.json`. Tests: `TrTaxParametersLoaderTest` (4), `TrTaxServiceTest` (11), `TrTaxControllerWebMvcTest` (4), `DividendRepositoryDataJpaTest` (+2 additive Docker-gated cases for the new aggregate queries), `useTrTaxReport.test.tsx` (2). Backend test count 1090 -> 1111 (+21); frontend 231 -> 233 (+2). `mvnw verify` green; JaCoCo 60% / 45%; Spotless clean. Frontend lint --max-warnings 0 + tsc strict + Vite build all clean. Three deviations: (a) OpenAPI spec regen deferred — `scripts/regen-openapi.sh` fails on a pre-existing `OpenTelemetry sdk-autoconfigure ComponentLoader` `NoClassDefFoundError` from 26-01 that ALSO fails at HEAD `bd6cf2a` (verified by re-running the script at the prior commit), so the regression is not introduced here and fixing it would expand scope (new dep). The new endpoint is exercised end-to-end by `TrTaxControllerWebMvcTest`. (b) WebMvc 401-without-auth case became a 5xx-with-null-principal documentation case because `@AutoConfigureMockMvc(addFilters = false)` (the existing FinTrack `@WebMvcTest` convention) bypasses the JWT chain — full end-to-end auth is covered by integration tests elsewhere. (c) `@Validated` controller-level dropped in favour of manual `IllegalArgumentException` to avoid adding a new `ConstraintViolationException` handler. `.env.example`, `docker-compose.yml`, `CHANGELOG.md` left untouched per project guards; CHANGELOG entry described in 27-01-SUMMARY.md and applied by the release flow. Operator runbook in `docs/OPERATIONS.md` -> `## Updating TR tax parameters (yearly)` covers the yearly maintenance workflow + new-locale extension pattern.
- 2026-05-07 (plan 27-02): Closed Phase 27 sub-plan 02 — accounts entity (G2-a). Standalone owner-scoped declaration of where value sits — bank checking / bank savings / brokerage cash / crypto wallet / physical cash — independent of `portfolios` and `transactions` in this plan; 27-03 wires the FK from `transactions` and `investment_transactions` to `accounts.id`. New Flyway migration `V41__accounts.sql` ships `accounts (id, user_id FK->users(id) ON DELETE CASCADE, name, account_type, currency, institution, account_number_suffix, notes, current_balance NUMERIC(20,8), is_archived, created_at)` with a partial unique index `uq_accounts_user_name_live ON (user_id, lower(name)) WHERE is_archived = FALSE` (live-rows-only duplicate-name guard so an archived `Main` does not block a new live `main`) and a hot-read companion `idx_accounts_user_archived ON (user_id, is_archived)`. `Account` JPA entity in `com.fintrack.common.entity` mirrors `Portfolio` line-for-line with `precision = 20, scale = 8` on `currentBalance` so `ddl-auto=validate` matches; the nested `AccountType` enum carries six values. Naming choice: `archived` (default false) instead of Portfolio's `active` (default true) — the persistence column is `is_archived BOOLEAN NOT NULL DEFAULT false`; the owner-facing UI says "Archive" / "Restore" so the field name reads cleaner than a double-negative. Decimal scale 8 chosen over 2 to round-trip Bitcoin satoshi-precision losslessly on the wire (BigDecimal serialises as string via Jackson); operator runbook documents whole-coin denomination for assets finer than 8 decimals. New `com.fintrack.account` feature package: `AccountRepository` (six methods: `findByUserIdAndArchivedFalseOrderByCreatedAtAsc`, `findByIdAndUserIdAndArchivedFalse`, `countByUserIdAndArchivedFalse`, `countByUserIdAndArchivedTrue`, `existsByUserIdAndNameIgnoreCaseAndArchivedFalse`, `sumBalancesByCurrencyForUser` JPQL `GROUP BY a.currency` aggregate), `AccountService` (list / get / create / update / delete soft-archive / totalsForUser; 50-account cap; case-insensitive duplicate-name guard restricted to live rows; type immutable post-create; currency mutable but not historically rebased; `currentUsername()` static helper copy of the 24-08 PortfolioService pattern; auditService.success after every write + auditService.failure before every BusinessRuleException throw, mirroring the 24-08 audit contract byte-for-byte), `AccountController` (six REST endpoints under `/api/v1/accounts`: list / totals / get / create / update / delete; `/totals` declared before `/{id}` so Spring's path matcher routes correctly; `@AuthenticationPrincipal FinTrackUserDetails` ownership scoping on every method), four DTO records (`AccountResponse`, `CreateAccountRequest`, `UpdateAccountRequest`, `AccountTotalsResponse` + nested `CurrencyTotal`). `AuditAction` grew by 3 constants (`ACCOUNT_CREATED`, `ACCOUNT_UPDATED`, `ACCOUNT_DELETED` — `*_DELETED` for the soft-archive op matches the 24-08 cross-entity convention). Tests: `AccountServiceTest` (14 cases — happy paths + name trim + currency uppercase + cap-50 limit + duplicate-name guard + null opening balance → zero + update field mutation + same-name-update skips guard + archive idempotence + totals-by-currency aggregation), `AccountServiceAuditTest` (6 cases — Mockito.verify of success/failure emission per mutation, mirroring `PortfolioServiceAuditTest` byte-for-byte), `AccountControllerWebMvcTest` (10 cases — list / totals / get / 404 / create-201 / 400-on-missing-name / 400-on-invalid-currency / 400-on-duplicate-name / update / delete-204), `AccountRepositoryDataJpaTest` (4 Docker-gated cases). Backend test count 1111 -> 1145 (+34, exceeds the +25 plan target). Frontend ships `/accounts` page lazy-loaded from `App.tsx`, sidebar link with `Landmark` lucide icon between Portfolios and Budget, six React Query hooks in `useAccounts.ts` (list / get / totals / create / update / archive — mutations invalidate both `['accounts']` and `['accounts', 'totals']` keys so the totals card reflects mutations without manual refetch), `accounts.api.ts` axios module, `account.types.ts` types file (BigDecimal serialised as string for precision per project precedent), `AccountListItem.tsx` + `AddAccountDialog.tsx` shadcn-based components (kebab menu with Edit / Archive actions; archive is a confirm-on-second-click pattern matching `PortfolioListItem`; the dialog handles both create and edit modes via a single `editing` prop). `useAccounts.test.tsx` ships 3 cases (list query key + create invalidates + archive invalidates list+totals). Frontend test count 233 -> 236 (+3). New `accounts.*` i18n namespace in both `tr.json` + `en.json` (types / form / dialog / actions / errors sub-namespaces; `nav.accounts` key added). Backend `mvnw verify` green; JaCoCo 60% / 45% met; Spotless clean. Frontend lint --max-warnings 0 + tsc strict + Vitest + Vite build all clean. Three load-bearing decisions: **(a)** `accounts` is **standalone in 27-02** — no FK from `transactions` or `investment_transactions`, no `account_id` column added anywhere outside the new `accounts` table. This is the explicit boundary between 27-02 and 27-03 and keeps the rollback to a single `V41` revert + a single `com.fintrack.account` package + a single frontend tree. **(b)** Multi-currency display is per-currency in 27-02 — the stat strip shows `TRY 102,500 · USD 3,200 · EUR 800` not a single TRY-equivalent total. True FX-converted rollup needs the price/FX snapshot service to expose a daily-rate lookup; flagged in "Deferred Enhancements". **(c)** No `@Observed` annotations — the 26-01 servlet observation handler auto-instruments every `@RestController` method; the service layer does sub-millisecond work, and `@Observed` belongs on hot batch paths like `PriceSyncService.refreshLive()`. One deviation: OpenAPI spec regen still defers — `scripts/regen-openapi.sh` fails on the same pre-existing 26-01 `OpenTelemetry sdk-autoconfigure ComponentLoader` `NoClassDefFoundError` documented in 27-01-SUMMARY (verified pre-existing at HEAD; not introduced here). The 23-03 contract gate will catch endpoint drift the moment the regen script is fixed; `AccountControllerWebMvcTest` covers the endpoint surface in the meantime. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. Operator runbook in `docs/OPERATIONS.md` -> `## Managing accounts` covers add / edit / archive workflow + 8-decimal precision note + 27-03 forward reference.
- 2026-05-09 (plan 27-03): Closed Phase 27 sub-plan 03 — transactions linked to accounts + emergency-fund coverage tile (G2-b). Two new Flyway migrations: `V42__transactions_account_fk.sql` adds nullable `account_id UUID REFERENCES accounts(id) ON DELETE SET NULL` to `transactions`, `investment_transactions`, `bill_payments` plus three partial indexes `WHERE account_id IS NOT NULL`; `V43__user_settings_emergency_fund_types.sql` adds `emergency_fund_include_types JSONB NOT NULL DEFAULT '["BANK_SAVINGS"]'`. Three transaction entities + `UserSettings` carry the new fields; `ddl-auto=validate` clean. Five event records widened: `BudgetTransactionPersistedEvent` (`accountId`, `previousAccountId`), new `BudgetTransactionDeletedEvent`, `InvestmentTransactionRecordedEvent` (`accountId`, `previousAccountId`), `InvestmentTransactionDeletedEvent` (`accountId`), `BillPaidEvent` (`accountId`, `previousStatus`, `previousAmount`, `previousAccountId`). New `com.fintrack.account.AccountTransactionAggregator` pure-static helpers (`budgetDelta`, `investmentDelta`, `billPaymentDelta`) cover every TxnType + PaymentStatus branch. New `AccountBalanceListener` registers five `@TransactionalEventListener(AFTER_COMMIT)` methods that delegate to `AccountBalanceUpdater` whose write method carries `@Transactional(propagation = Propagation.REQUIRES_NEW)` — the split is load-bearing because Spring's event-listener proxy bypasses self-invocation `@Transactional`. Listener catches `RuntimeException` and logs without rethrowing so the writer's commit cannot roll back. `BudgetService` / `InvestmentTransactionService` / `BillService` thread `accountId` through every write path with ownership guards (`accountRepository.findByIdAndUserIdAndArchivedFalse(accountId, userId)` + `BusinessRuleException("ACCOUNT_NOT_OWNED")` + `auditService.failure` on absent), publish events with the new fields, and surface `accountId` + `accountName` on response DTOs via batched account lookups (no N+1). `EmergencyFundService.compute(userId)` returns `(currentReserve, buckets, monthlyAverageExpense, monthsCovered, status, includedTypes, sampleMonths)` with the four-status band (red < 3 / amber 3-6 / green > 6 / insufficient-data when samples < 3). New `EmergencyFundController` exposes `GET /api/v1/dashboard/emergency-fund` and `PUT /api/v1/dashboard/emergency-fund/types`. `SettingsService.updateEmergencyFundTypes` enforces `BANK_SAVINGS` membership at the service layer. New backend test suites: `AccountTransactionAggregatorTest` (16), `AccountBalanceUpdaterTest` (6), `AccountBalanceListenerTest` (15), `EmergencyFundServiceTest` (10), `EmergencyFundControllerWebMvcTest` (4); additive cases on `BudgetServiceTest` (+5), `InvestmentTransactionServiceTest` (+3), `BillServiceTest` (+3). Frontend ships `EmergencyFundCard` above `FireCalculatorCard` on the dashboard with red/amber/green/insufficient band, tri-state include-types toggle (BANK_SAVINGS locked-on; BANK_CHECKING / CASH user-toggleable), and a face-value cross-currency disclaimer. Shared `AccountPicker.tsx` mounted in budget + investment transaction dialogs with a "(no account)" default. New `useEmergencyFund` + `useUpdateEmergencyFundTypes` React Query hooks. New `emergencyFund.*` i18n namespace in both `tr.json` and `en.json`; existing `accounts.label` / `accounts.noAccount` keys added. Backend `mvnw verify` green (1207 tests passing — 1145 -> 1207, +62, exceeds the +35 plan target); JaCoCo 60%/45% met; Spotless clean. Frontend lint --max-warnings 0 + tsc strict + Vite build all clean; 239 tests passing (236 -> 239, +3 — meets the +3 plan target). Three load-bearing decisions: **(a)** Nullable FK + null-as-out-of-band semantics — every existing row stays at NULL; the aggregator skips null-account events; zero auto-assignment of historical rows. **(b)** `@TransactionalEventListener(AFTER_COMMIT)` + `REQUIRES_NEW` propagation via separate `AccountBalanceUpdater` bean — Spring's event proxy bypasses self-invocation `@Transactional`, so the dedicated bean is the only way to honour `REQUIRES_NEW`. **(c)** Face-value cross-currency rollup deferred to 28-01 — the frontend tile carries a per-currency bucket breakdown so the operator can interpret the number; the threshold colour bands apply to `monthsCovered` against the user's home currency expenses. Two deviations: (i) The bill-pay flow's account picker is omitted (no `PayBillDialog` exists; `BillsPage` invokes pay inline); the listener still rolls bill-payment events when `accountId` is non-null on the request DTO, so the backend contract is complete. Documented in 27-03-SUMMARY's "Deferred Enhancements". (ii) OpenAPI spec regen still defers — `scripts/regen-openapi.sh` fails on the same pre-existing 26-01 OpenTelemetry `ComponentLoader` `NoClassDefFoundError` from 27-01 / 27-02 (verified pre-existing at HEAD); the `BudgetTransaction` and `Bill` keyof contract tests in `frontend/src/api/__contract__/api-contract.test.ts` widen the expected key set with `'accountId' | 'accountName'` so the contract gate accepts the new fields ahead of the OpenAPI regen. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. Operator runbook in `docs/OPERATIONS.md` -> `## Linking transactions to accounts` + `### Emergency-fund coverage`.
- 2026-05-09 (plan 28-02): Closed Phase 28 sub-plan 02 — rebalance executor (G12). New Flyway migration `V46__user_settings_rebalance_drift_threshold.sql` adds `rebalance_drift_threshold_percent NUMERIC(5,2) NOT NULL DEFAULT 1.00 CHECK (BETWEEN 0.10 AND 10.00)` to `user_settings`. New `com.fintrack.portfolio.rebalance` feature package: `RebalanceService` (`@Observed`-annotated preview + commit + bucket-to-holding projection algorithm + cash scaling + integer-truncation + SHA-256 canonical hash), `RebalanceController` mounting `POST /api/v1/portfolios/{id}/rebalance/preview` + `POST .../commit`, four DTO records, `RebalanceProposalStore` Redis adapter (30-minute open-proposal TTL, 24-hour committed-marker TTL). New `RebalanceConflictException` extends `BusinessRuleException`; `GlobalExceptionHandler` maps it to HTTP 409 so stale-preview and double-commit return Conflict instead of Bad Request while every other `BusinessRuleException` stays on 400. `AuditAction` grew by 3 constants (`REBALANCE_PREVIEWED`, `REBALANCE_COMMITTED`, `USER_SETTINGS_REBALANCE_THRESHOLD_UPDATED`). `SettingsService.updateRebalanceDriftThreshold` + `PUT /api/v1/settings/rebalance-threshold` (folded into the existing `SettingsController` per the 28-01 precedent) close the threshold-write path with Bean Validation + service-layer guard + audit emission. The bucket-to-holding projection: per-bucket drift via `AllocationService.summarize` semantics → filter buckets where `|drift| <= threshold` → OVERWEIGHT bucket = pro-rata SELL across the bucket's holdings (capped at holding value); UNDERWEIGHT bucket = single-holding BUY on the highest-value existing holding (alphabetical tiebreak by symbol); empty underweight bucket emits a `NO_HOLDING_TO_BUY` informational row. Cash scaling: BUY rows are proportionally scaled when `requiredBuy > availableCash` (summary warning `CASH_PARTIAL_SCALEDOWN`); SELL rows are NEVER scaled. Quantity quirks: STOCK / FUND truncate to integer via `RoundingMode.DOWN`; truncated-to-zero rows gain `QUANTITY_BELOW_LOT`; CRYPTO / GOLD / CURRENCY / OTHER keep scale=8. Hash check: preview computes `SHA-256(portfolioId|accountId|[per suggestion: assetId:action:quantity:price])` and stores in Redis under `rebalance:proposal:<userId>:<proposalId>` with 30-minute TTL; commit recomputes from the live state and rejects on mismatch via `RebalanceConflictException(REBALANCE_PROPOSAL_STALE)`; double-commit within 24h returns `REBALANCE_PROPOSAL_ALREADY_COMMITTED` via the `rebalance:committed:` sentinel. Materialisation reuses `InvestmentTransactionService.record(...)` so the existing audit (`INVESTMENT_TRANSACTION_CREATED` per row), 25-01 `HoldingProjectionListener`, and 27-03 `AccountBalanceListener` all fire normally. Two `@Observed` annotations on the service-orchestrator methods (`portfolio.rebalance.preview` / `portfolio.rebalance.commit`) wire to the 26-01 observation registry. Frontend ships `RebalanceCard` mounted inline on `PortfolioDetailPage` AFTER the `AllocationTargets` card (NOT a new route), gated behind `allocation.configured && holdings.length > 0`; the card surfaces a drift-threshold slider (400ms debounce → settings PUT), shared `AccountPicker`, `Generate suggestions` button, suggestion table with per-row checkboxes (default-checked for BUY/SELL with no warning, default-unchecked for warning rows), and `Commit selected (N rows)` button. New `useRebalance.ts` hook trio (`useRebalancePreview`, `useRebalanceCommit`, `useUpdateRebalanceThreshold`); the commit invalidates five cache keys (`['portfolios', portfolioId, 'holdings']` / `['transactions', portfolioId]` / `['allocation', portfolioId]` / `['accounts']` / `['accounts', 'totals']`) so the dashboard reflects the rebalance batch without manual refetch. New `rebalance.*` i18n namespace in both `tr.json` + `en.json` (header / description / slider / actions / warnings / errors / summary / columns / success toast). Backend test count 1294 -> 1340 (+46, meets plan target): `RebalanceServiceTest` (22 cases — bucket-delta computation, pro-rata SELL distribution, cash scaling, quantity quirks, integer-truncation for STOCK/FUND, no-holding-to-buy warning, full preview/commit happy paths, stale proposal rejection, replay rejection, missing targets, account ownership guard, audit emission, hash determinism, hash sensitivity to accountId), `RebalanceControllerWebMvcTest` (11 cases — preview happy 200 / preview 400 missing accountId / preview 404 unknown portfolio / preview 400 invalid threshold override / commit happy 200 / commit 409 stale / commit 409 already committed / commit 404 unknown proposalId / commit 400 empty selections / commit 400 selections out of range / preview 400 empty JSON), `RebalanceProposalStoreTest` (5 cases — TTL on putProposal / read happy / read absent / markCommitted writes sentinel + deletes open key / isCommitted reflects key presence), `SettingsServiceRebalanceThresholdTest` (4 cases — happy / below min / above max / audit emission), `SettingsControllerWebMvcTest` (+3 cases), `UserSettingsRepositoryDataJpaTest` (+2 Docker-gated cases). Frontend test count 247 -> 255 (+8, meets target): `useRebalance.test.tsx` (4 cases — preview call shape / commit invalidates dependent caches / threshold mutation / preview surfaces server errors), `RebalanceCard.test.tsx` (4 cases — hidden when allocation.configured=false / generate disabled without account / preview renders rows / commit posts only ticked indices). `mvnw verify` green; JaCoCo 60% / 45% met; Spotless clean. Frontend lint --max-warnings 0 + tsc strict + Vitest + Vite build all clean. Three load-bearing decisions: **(a)** `RebalanceConflictException` as a new `BusinessRuleException` subclass — the default 400 mapping in `GlobalExceptionHandler` is wrong for stale-preview / double-commit which are conceptually 409 Conflict; the new subclass-specific handler runs before the parent handler so 409 is returned only for those two codes while every other rebalance error (`REBALANCE_NO_TARGETS`, `REBALANCE_THRESHOLD_OUT_OF_RANGE`, `REBALANCE_SELECTION_OUT_OF_RANGE`, `ACCOUNT_NOT_OWNED`) stays on 400. **(b)** Pro-rata SELL + single-holding BUY projection — the simplest correct algorithm; pro-rata SELL preserves within-bucket share, single-holding BUY minimises commission/fee count, and an empty underweight bucket emits a `NO_HOLDING_TO_BUY` row instead of recommending a new asset (the executor is corrective, not a stock picker). **(c)** Cash scaling is proportional, not iterative — when `requiredBuy > availableCash`, every BUY row scales by the same `availableCash / requiredBuy` ratio rather than filling the highest-priority bucket first. The proportional rule preserves the operator's relative bucket priorities under partial-cash. OpenAPI regen still defers per the pre-existing 26-01 OpenTelemetry `ComponentLoader` `NoClassDefFoundError`; `RebalanceControllerWebMvcTest` + `SettingsControllerWebMvcTest` cover the new endpoint surface in the meantime. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md gains `## Rebalancing a portfolio` H2 covering the preview+commit workflow, drift-threshold setting, cash-scaling behaviour, warning interpretations, integer-quantity quirk, proposal-expiry / stale-preview reasoning, and audit-log forensic queries. Phase 28 complete (2/2 plans).
- 2026-05-09 (plan 28-01): Closed Phase 28 sub-plan 01 — configurable emergency-fund target months (G11). New Flyway migration `V45__user_settings_emergency_fund_targets.sql` adds `emergency_fund_target_months SMALLINT NOT NULL DEFAULT 6 CHECK (BETWEEN 2 AND 24)` and `emergency_fund_amber_floor_months SMALLINT NOT NULL DEFAULT 3 CHECK (BETWEEN 1 AND 23)` to `user_settings`. The cross-column invariant `amber_floor < target_months` lives at the service layer (V45 ships single-column CHECKs only — multi-column CHECKs are awkward to evolve later). `UserSettings` JPA entity gains two `Short` fields with `@Builder.Default` matching the SQL defaults. `EmergencyFundService.compute(userId)` reads the configured pair via a defensive null-fallback (`DEFAULT_TARGET_MONTHS=6`, `DEFAULT_AMBER_FLOOR=3`) and recomputes bands against the user-configured pair: `monthsCovered < amberFloor` -> red, `<= target` -> amber (inclusive on both edges), `> target` -> green. The legacy 6/3 behaviour is preserved exactly when the columns hold the migration defaults. `EmergencyFundResponse` DTO widens by two `int` fields (`targetMonths`, `amberFloorMonths`) at the END so existing JSON consumers do not break on field-order assumptions. New `SettingsService.updateEmergencyFundConfig(userId, types, targetMonths, amberFloorMonths)` writes types + target + amber-floor atomically with three validation gates (BANK_SAVINGS membership / target in `[2, 24]` / amber-floor in `[1, target - 1]`); legacy `updateEmergencyFundTypes` becomes a thin delegate that re-uses the operator's existing target / amber-floor. New `EmergencyFundController.updateConfig` mounts `PUT /api/v1/dashboard/emergency-fund/config` accepting `UpdateEmergencyFundConfigRequest(types, targetMonths, amberFloorMonths)` with Bean Validation (`@NotNull @Size(1, 6)` types, `@Min(2) @Max(24)` targetMonths, `@Min(1) @Max(23)` amberFloorMonths); the cross-field invariant lives at the service layer per the 27-03 precedent. `AuditAction` grew by 1 constant (`USER_SETTINGS_EMERGENCY_FUND_UPDATED`) emitted on success and on every failure throw site, mirroring the 24-08 audit-failure-first contract. Frontend `EmergencyFundCard` gains a stepper pair for `targetMonths` (range `[max(2, amberFloor + 1), 24]`) and `amberFloorMonths` (range `[1, target - 1]`) inside a new "Reserve target" sub-card. New `useUpdateEmergencyFundConfig` React Query mutation shares the `['dashboard', 'emergency-fund']` cache key so the steppers and the Settings section reflect each other immediately. Status copy switches to dynamic i18n with `{{target}}` / `{{amber}}` interpolation (`bandRedDynamic` / `bandAmberDynamic` / `bandGreenDynamic`); legacy static `statusRed` / `statusAmber` / `statusGreen` keys removed. New `EmergencyFundSection.tsx` mounts on `SettingsPage` between `PushNotificationSection` and `TagsSection` (icon `ShieldCheck`), with `Input type="number"` steppers + the type-toggle chips. Backend tests delta covered by new test cases on `EmergencyFundServiceTest` (existing 10 stay green; configurable-band cases pin custom target / custom amber-floor / target boundary inclusivity / amber-floor boundary inclusivity / null-column fallback / response-carries-targets), `EmergencyFundControllerWebMvcTest` (existing 4 stay green via DTO widening; new cases pin happy path + 400 on out-of-range target + 400 on empty types + 400 on null target), and `SettingsServiceEmergencyFundConfigTest` (validation + audit-emission contract). Frontend test count delta `useEmergencyFund.test.tsx` (+2 cases for config mutation + legacy-types delegation) + new `EmergencyFundSection.test.tsx` (3 cases — initial render / target stepper clamps amber-floor / BANK_SAVINGS chip locked). Three load-bearing decisions: **(a)** Service-layer validation over multi-column DB CHECK — V45 ships single-column CHECKs only because relaxing them later via a fresh migration is awkward, while the `amber_floor < target` invariant lives in `SettingsService` per the 27-03 `BANK_SAVINGS`-membership precedent. **(b)** Range bounds 2-24 for target months and 1-23 for amber-floor — a 1-month emergency fund is functionally "no buffer", and beyond 24 months the question stops being "emergency fund" and becomes "wealth allocation". **(c)** Cross-currency / FX rollup is OUT OF SCOPE — the FX-rate snapshot service is non-trivial (daily ECB/TCMB fetcher, rate cache, asset-currency conversion, additional UI) and orthogonal to the configurable-N piece; deferred to a future Phase 28 / 29 plan. The `crossCurrencyNote` i18n string updates to "FX-converted rollup deferred to a future Phase 28 plan" so the UI does not lie. Frontend lint --max-warnings 0 + tsc strict + Vitest all clean. OpenAPI regen still defers per the pre-existing 26-01 OpenTelemetry `ComponentLoader` `NoClassDefFoundError`; `EmergencyFundControllerWebMvcTest` covers the new endpoint surface in the meantime. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md `### Emergency-fund coverage` extends with `#### Configuring target months`.
- 2026-05-09 (plan 27-04): Closed Phase 27 sub-plan 04 — TR bank CSV statement importer (G2-c). Owner uploads a month's bank statement at `/imports/bank-csv`, picks the bank + target account, previews the parsed rows + matched categories + duplicate count, then commits. Inserted rows land as `BudgetTransaction`s with `account_id` stamped, the 27-03 `AccountBalanceListener` recomputes the account's `current_balance` after commit, and re-uploading the same file is a no-op. New Flyway migration `V44__bank_csv_import_fingerprint.sql` adds `import_fingerprint VARCHAR(64)` (SHA-256 over `accountId|txnDate|amount|description`) on `transactions` plus a partial unique index `uq_transactions_account_fingerprint ON (account_id, import_fingerprint) WHERE account_id IS NOT NULL AND import_fingerprint IS NOT NULL` so dedupe is enforced at the DB level for account-scoped rows only. Three parsers ship in `com.fintrack.imports.bank` keyed by an enum `Bank` (GARANTI / ISBANK / AKBANK): `GarantiCsvParser` (Windows-1254, semicolon, `dd/MM/yyyy`, `1.234,56`), `IsbankCsvParser` (UTF-8, comma, `dd.MM.yyyy`, `1.234,56`; supports both legacy + international header variants), `AkbankCsvParser` (UTF-8, semicolon, `dd.MM.yyyy`, `1234,56`). `BankCsvParserRegistry` `@Component` injects the three parsers via Spring's auto-discovered `Map<Bank, BankCsvParser>` and throws `IllegalArgumentException` on unknown bank. Each parser produces a `RawBankRow(LocalDate txnDate, BigDecimal amount, String description)` list and surfaces parse errors as a non-fatal warnings list so a single corrupt row does not abort the import. New `BankCsvCategoryMatcher` resolves the category for each row by walking the operator's existing `TransactionCategoryRule` set with case-insensitive `Pattern.compile`; a row that matches nothing lands with NULL category and is visible in `/budget` with the `(uncategorised)` filter. `BankCsvImportService` exposes `preview(userId, bank, accountId, file)` and `commit(userId, bank, accountId, file)` — both fan out parser → category match → fingerprint compute, then preview returns a summary DTO without writing while commit inserts only the non-duplicate rows in a single `@Transactional` block, audits the action via `auditService.success(BANK_CSV_PREVIEWED|BANK_CSV_COMMITTED)`, and surfaces `BANK_CSV_INVALID` (parse failure / empty file / wrong bank) and `ACCOUNT_NOT_OWNED` (archived or wrong-user account) via `BusinessRuleException`. New `BankCsvImportController` exposes `POST /api/v1/imports/bank/preview` and `POST /api/v1/imports/bank/commit` as multipart endpoints. `GlobalExceptionHandler` gains `@ExceptionHandler` cases for `MissingServletRequestParameterException` and `MissingServletRequestPartException` mapping both to 400 with the consistent `ErrorResponse` envelope. Five new test fixtures live at `backend/src/test/resources/bank-csv/`. Tests: `GarantiCsvParserTest` (parametrised), `IsbankCsvParserTest`, `AkbankCsvParserTest`, `BankCsvCategoryMatcherTest`, `BankCsvImportServiceTest`, `BankCsvImportControllerWebMvcTest`, plus 5 additive cases on `TransactionRepositoryDataJpaTest` for the fingerprint round-trip + partial-index dedupe. Backend test count 1207 -> 1275 (+68, exceeds the +35 plan target). Frontend ships a lazy-loaded `/imports/bank-csv` page with bank dropdown + account picker + file upload + preview pane (row count + first-N rows + matched categories + duplicate count + warnings) + commit button, sidebar link, `useBankCsvPreview` + `useBankCsvCommit` React Query hooks (the commit invalidates `['accounts']`, `['accounts', 'totals']`, and `['budget']` keys so the dashboard reflects the import without manual refetch), `bankcsv.api.ts` axios module with multipart `FormData` builder, `bankCsv.types.ts` types module, full `bankCsvImport.*` i18n namespace in both `tr.json` and `en.json`. Frontend test count 239 -> 242 (+3, meets the plan target). Backend `mvnw verify` green; JaCoCo 60%/45% met; Spotless clean. Frontend lint --max-warnings 0 + tsc strict + Vitest + Vite build all clean. Three load-bearing decisions: **(a)** Per-account fingerprint dedupe via partial unique index — the fingerprint is account-scoped not user-scoped, so the same row across two different accounts is NOT a duplicate (e.g. an inter-account transfer the bank exports on both sides). The partial index `WHERE account_id IS NOT NULL AND import_fingerprint IS NOT NULL` keeps pre-27-04 NULL rows untouched. **(b)** Parser warnings are non-fatal — a single unparseable row surfaces as `parserWarnings: [{row: 7, message: "..."}]` on the preview/commit DTO without aborting the import. The operator can decide whether to re-export from the bank or accept the partial set. **(c)** Categorisation reuses the existing `TransactionCategoryRule` set verbatim — no new rule schema, no per-bank rule taxonomy. The matcher walks rules in order and stops at the first match; rules already drive in-app create/edit so the import surface stays consistent with manual entry. One deviation: pre-existing `BudgetControllerWebMvcTest.listTransactionsRejectsMissingMonth` was asserting `is5xxServerError()` on a missing `?month` param. The new `MissingServletRequestParameterException` handler (added in Task 5 to give the bank-CSV multipart endpoint a clean 400) now correctly maps the missing-param case to 400, so the assertion was updated to `isBadRequest()` — the test name `Rejects` was always the load-bearing intent; the prior 5xx was a side-effect of no handler being registered. Two cross-cutting deviations carry forward unchanged: (i) OpenAPI spec regen still defers per the pre-existing 26-01 OpenTelemetry `ComponentLoader` `NoClassDefFoundError` (verified at HEAD) — the new `/imports/bank/*` endpoint surface is exercised end-to-end by `BankCsvImportControllerWebMvcTest`. (ii) Credit-card statements / FX sub-accounts / brokerage cash statements are out of scope; the v1 parsers cover only TR current accounts. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards; CHANGELOG entry described in 27-04-SUMMARY.md and applied by the release flow. Operator runbook in `docs/OPERATIONS.md` -> `## Importing TR bank CSV statements` covers the monthly workflow + per-bank export options + idempotency note + categorisation note + troubleshooting.
- 2026-05-07 (plan 26-02): Closed Phase 26 sub-plan 02 — self-hosted GlitchTip with release tagging. Picked GlitchTip over Sentry-self-hosted on homelab footprint (2 containers vs ~25; reuses existing Postgres + Redis on logical DB 1; PROJECT.md "no premature distributed-systems gear" rules out Sentry's full ~25-container stack). io.sentry:sentry-spring-boot-starter-jakarta:7.18.0 wired on the backend POM (Spring Boot 3.x jakarta line). New sentry: block in application.yml with traces-sample-rate=0.0 (Tempo from 26-01 owns trace data), send-default-pii=false, attach-stacktrace=true, max-request-body-size=none, in-app-includes=com.fintrack; production profile overrides logging.minimum-event-level=warn. New ReleaseInfoConfig.releaseVersionSupplier reads IDENTITY.yaml lazily via SnakeYAML, memoises via a hand-rolled volatile field, falls back to fintrack@unknown on missing file with one WARN log line; FINTRACK_RELEASE_VERSION env override wins via Spring placeholder. New SentryConfig registers a BeforeSendCallback that scrubs every event message + breadcrumb through the existing AuditPiiRedactor (24-05's single redaction policy) and attaches trace.id / span.id (from Micrometer Tracer.currentSpan()) plus request.id (from MDC populated by RequestLoggingFilter) tags so events cross-link to Tempo trace trees by exact id. ProductionProfileGuard now requires SENTRY_DSN under the production profile, aggregated into the existing IllegalStateException payload (24-07's single-shot remediation list — 6 -> 7 violations). GlitchTip stack ships as a SEPARATE compose overlay at monitoring/glitchtip/docker-compose.glitchtip.yml — main docker-compose.yml is untouched (project's pre_guard_release_files.py PreToolUse hook). Overlay declares fintrack-net as external: true so it joins the existing network, merges a single volumes: entry onto postgres for the init-script mount (monitoring/glitchtip/init-db.sql, idempotent DO blocks + \gexec creating a dedicated glitchtip role + database), and adds glitchtip-web (Django app, port 8000 internal) + glitchtip-worker (Celery + beat) services + a glitchtip-uploads named volume. Operator brings it up with `docker compose -f docker-compose.yml -f monitoring/glitchtip/docker-compose.glitchtip.yml up -d`. Backend env injection (SENTRY_DSN, FINTRACK_RELEASE_VERSION) goes via the operator's existing .env (compose's automatic interpolation), NOT main-compose environment: — load-bearing because docker-compose.yml is locked. Operator-facing env vars + the canonical overlay invocation + first-boot init runbook documented in docs/OPERATIONS.md (new ## GlitchTip / Sentry release tagging H2 section); .env.example is untouched per project deny rule (Write/Edit(**/.env.*)). Tests: SentryConfigTest (5 — PII scrub on event messages + breadcrumbs using the real AuditPiiRedactor, trace.id/span.id tag injection from a mocked Tracer, no-op fallback when Tracer is null, request.id tag from MDC + empty-MDC negative case), ReleaseInfoConfigTest (3 — IDENTITY.yaml happy path against a 9.9.9 fixture, missing-file fallback with exactly one WARN via Logback ListAppender, reference-equal memoisation), ProductionProfileGuardTest +2 (enforce_failsWhenSentryDsnBlank, enforce_passesWithAllProductionEnvSet) plus existing 6 with sentry.dsn populated. Test count 1073 -> 1083 (+10). mvnw verify green; JaCoCo 60%/45% met; Spotless clean. Three deviations: (a) sentry-spring-boot-starter-jakarta:7.18.0 transitive tree pulls 4 artefacts (starter + spring-boot-jakarta + spring-jakarta + core), NOT including sentry-logback / sentry-jul as the plan's verify expected — the spring-jakarta starter's auto-config alone covers GlobalExceptionHandler + scheduler exception capture; (b) SentryConfigTest + ReleaseInfoConfigTest written as direct unit tests, not @SpringJUnitConfig slices — both classes have minimal collaborators and direct construction exercises the same load-bearing behaviour at lower cost; (c) CHANGELOG.md is additionally covered by the pre_guard_release_files.py PreToolUse hook, applied via a one-shot Python script per the 26-01 precedent.

### Deferred Issues

See `tasks/ROADMAP.md` "Won't do" list and Track G items not yet phased (G7-G10, G13-G16).
- ISS-100..ISS-109: per-class mutation lift backlog for service classes still below the 60% per-class target.
- ISS-110: full WebAuthn ceremony E2E test via `com.webauthn4j.test.client.ClientPlatform`.
- ISS-111: TagService + AllocationService audit emission continuation.

### Blockers/Concerns

From `.planning/codebase/CONCERNS.md`:
- `WebClient.block()` + `Thread.sleep` in price clients → Phase 25 [resolved by 25-03]
- Permissive CORS in production profile → Phase 24 [resolved by 24-07]
- Optional Redis password → Phase 24 [resolved by 24-07]
- AuditService coverage for domain mutations → Phase 24 [resolved by 24-08]
- Per-asset delta WebSocket broadcast → Phase 30

Notes:
- 27-01 added the TR tax helper at `/reports/tax/tr`; the year-keyed YAML at `backend/src/main/resources/tax/tax-parameters-tr.yml` is updated yearly per `docs/OPERATIONS.md` -> "Updating TR tax parameters (yearly)".
- 27-02 added the standalone `accounts` entity at `/accounts` with REST CRUD under `/api/v1/accounts`; 27-03 wires `transactions` and `investment_transactions` to `account_id`.
- 27-04 added the TR bank CSV statement importer at `/imports/bank-csv`; rows land as `BudgetTransaction`s with `account_id` stamped, the 27-03 `AccountBalanceListener` recomputes the account's `current_balance` after commit, and re-uploading the same file is a no-op via the V44 `import_fingerprint` partial unique index. Three parsers ship: GARANTI / ISBANK / AKBANK. Categorisation reuses the operator's existing `TransactionCategoryRule` regex set; rows that match nothing land NULL and are visible in `/budget` with the `(uncategorised)` filter.
- `scripts/regen-openapi.sh` is broken by a pre-existing OpenTelemetry sdk-autoconfigure `ComponentLoader` `NoClassDefFoundError` from 26-01 (also affects pre-27-01, pre-27-02, pre-27-03, and pre-27-04 commits). Fix should be its own follow-up plan; the 23-03 contract test will catch endpoint drift the moment the regen script is fixed.

## Session Continuity

Last session: 2026-05-09
Stopped at: Phase 28 complete (2/2 plans). Plan 28-02 shipped the rebalance executor (Track G12). New Flyway migration `V46__user_settings_rebalance_drift_threshold.sql` adds a `NUMERIC(5,2)` column to `user_settings` with a single-column CHECK (`BETWEEN 0.10 AND 10.00`, default `1.00`) and a matching `BigDecimal` JPA field with `@Builder.Default`. New feature package `com.fintrack.portfolio.rebalance` with `RebalanceService` (preview + commit + bucket-to-holding projection algorithm), `RebalanceController` (`POST .../preview` + `POST .../commit`), four DTO records, and `RebalanceProposalStore` Redis adapter (30-minute open-proposal TTL + 24-hour committed-marker sentinel). New `RebalanceConflictException` mapped to HTTP 409 by `GlobalExceptionHandler` for stale-preview + double-commit (every other `BusinessRuleException` stays on 400). New `SettingsService.updateRebalanceDriftThreshold` + `PUT /api/v1/settings/rebalance-threshold` (folded into existing `SettingsController`). Three new `AuditAction` constants. The projection algorithm: per-bucket drift via `AllocationService.summarize` semantics → filter buckets within threshold → OVERWEIGHT = pro-rata SELL across bucket holdings; UNDERWEIGHT = single-holding BUY on highest-value existing holding (alphabetical tiebreak); empty underweight bucket emits `NO_HOLDING_TO_BUY` informational row. Cash scaling: BUY rows scale proportionally when required > available; SELL rows never scale. Quantity quirks: STOCK / FUND truncate to integer; CRYPTO / GOLD / CURRENCY / OTHER keep scale=8. Hash check: SHA-256 over `(portfolioId, accountId, suggestions[])` stored in Redis; commit recomputes from live state and rejects mismatches. Materialisation reuses `InvestmentTransactionService.record(...)` so 25-01 holding projection + 27-03 account-balance listener fire normally. Frontend `RebalanceCard` on `PortfolioDetailPage` (after `AllocationTargets`, gated behind `allocation.configured && holdings.length > 0`) with drift slider (400ms debounce), `AccountPicker`, generate button, suggestion table with per-row checkboxes, commit button. New `useRebalance.ts` trio + `rebalance.api.ts` + `rebalance.types.ts` + `rebalance.*` i18n namespace. Backend tests 1294 -> 1340 (+46, meets target); frontend 247 -> 255 (+8, meets target). `mvnw verify` green; JaCoCo 60% / 45%; Spotless clean. Frontend lint --max-warnings 0 + tsc strict + Vitest + Vite build all clean. OpenAPI regen still defers per the pre-existing 26-01 OpenTelemetry `ComponentLoader` `NoClassDefFoundError`; `RebalanceControllerWebMvcTest` + extended `SettingsControllerWebMvcTest` cover the new endpoint surface in the meantime. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md gains `## Rebalancing a portfolio` H2.
Resume file: Phase 29 in progress (1/3 plans). 29-01 shipped portfolio comparison (Track G4) — next 29-02 / 29-03 plans cover the remaining analytics tracks.
Earlier session: 2026-05-09
Stopped at: Phase 28 in progress (1/2 plans). Plan 28-01 shipped configurable emergency-fund target months (Track G11). New Flyway migration `V45__user_settings_emergency_fund_targets.sql` adds two SMALLINT columns (`emergency_fund_target_months` default 6 / range 2-24, `emergency_fund_amber_floor_months` default 3 / range 1-23) to `user_settings`. The cross-column invariant `amber_floor < target_months` lives at the `SettingsService` layer per the 27-03 `BANK_SAVINGS`-membership precedent. `EmergencyFundService.compute(userId)` reads the configured pair via a defensive null-fallback and recomputes bands against the user-configured pair (`< amberFloor` -> red, `<= target` -> amber inclusive, `> target` -> green); the legacy 6/3 behaviour is preserved exactly when the columns hold the migration defaults. `EmergencyFundResponse` widens by two `int` fields (`targetMonths`, `amberFloorMonths`) at the END so existing consumers do not break. New `SettingsService.updateEmergencyFundConfig` writes all three fields atomically with full validation; legacy `updateEmergencyFundTypes` becomes a thin delegate. New `EmergencyFundController.updateConfig` mounts `PUT /api/v1/dashboard/emergency-fund/config` with Bean Validation; the legacy `PUT .../types` stays in place. `AuditAction` grew by 1 constant (`USER_SETTINGS_EMERGENCY_FUND_UPDATED`). Frontend ships a stepper pair on `EmergencyFundCard` (target + amber-floor) and a new `EmergencyFundSection` on `SettingsPage` between `PushNotificationSection` and `TagsSection`. Status copy switches to dynamic i18n with `{{target}}` / `{{amber}}` interpolation; legacy static keys removed. Frontend lint --max-warnings 0 + tsc strict + Vitest all clean. OpenAPI regen still defers per the pre-existing 26-01 OpenTelemetry issue; `EmergencyFundControllerWebMvcTest` covers the new endpoint surface in the meantime. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md `### Emergency-fund coverage` extends with `#### Configuring target months`.
Resume file: Phase 28 in progress (1 / 2 plans). Next sub-plan 02 not started — rebalance executor (Track G12). Run `/gsd:plan-phase 28 02`.
Previous session: 2026-05-09
Stopped at: Phase 27 complete (4/4 plans). Plan 27-04 shipped the TR bank CSV statement importer (Track G2-c). New Flyway migration `V44__bank_csv_import_fingerprint.sql` adds `import_fingerprint VARCHAR(64)` to `transactions` plus a partial unique index `WHERE account_id IS NOT NULL AND import_fingerprint IS NOT NULL` for per-account dedupe; pre-27-04 NULL rows untouched. Three new parsers ship under `com.fintrack.imports.bank` keyed by an enum `Bank` (GARANTI / ISBANK / AKBANK), discovered via Spring's `Map<Bank, BankCsvParser>` injection in `BankCsvParserRegistry`. New `BankCsvCategoryMatcher` walks the operator's existing `TransactionCategoryRule` set with case-insensitive `Pattern.compile`. New `BankCsvImportService.preview/commit` exposes a dry-run + atomic-insert pair with audit emission (`BANK_CSV_PREVIEWED` / `BANK_CSV_COMMITTED`) and `BANK_CSV_INVALID` / `ACCOUNT_NOT_OWNED` error codes. New `BankCsvImportController` mounts two multipart `POST` endpoints. `GlobalExceptionHandler` gains 400 mappings for `MissingServletRequestParameterException` + `MissingServletRequestPartException`. Frontend ships `/imports/bank-csv` lazy-loaded page with bank dropdown + account picker + preview pane + commit button + sidebar link. Backend tests 1207 -> 1275 (+68, exceeds +35 plan target); frontend 239 -> 242 (+3, meets target). `mvnw verify` green; JaCoCo 60% / 45%; Spotless clean. Frontend lint + typecheck + build + Vitest all clean. One pre-existing test assertion drift fixed: `BudgetControllerWebMvcTest.listTransactionsRejectsMissingMonth` switched from `is5xxServerError()` to `isBadRequest()` to match the now-correct API behaviour after the missing-param handler addition. OpenAPI regen still defers per the pre-existing 26-01 issue; `BankCsvImportControllerWebMvcTest` covers the new endpoint surface in the meantime. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md gains `## Importing TR bank CSV statements`.
Earlier session: 2026-05-09
Stopped at: Phase 27 in progress (3 of 4 plans). Plan 27-03 wired transactions to accounts and shipped the emergency-fund coverage tile (Track G2-b). V42 + V43 Flyway migrations add nullable `account_id` FK columns to `transactions` / `investment_transactions` / `bill_payments` (with partial indexes `WHERE account_id IS NOT NULL`) plus a JSONB `emergency_fund_include_types` column on `user_settings`. Five event records carry `accountId` / `previousAccountId` / `previousStatus` / `previousAmount` where applicable. New `AccountBalanceListener` + `AccountBalanceUpdater` honour `REQUIRES_NEW` via a separate Spring bean (the event-listener proxy bypasses self-invocation `@Transactional`). New `AccountTransactionAggregator` pure-static helpers cover every TxnType + PaymentStatus branch. `BudgetService` / `InvestmentTransactionService` / `BillService` thread `accountId` end-to-end with ownership guards + audit-detail enrichment. New `EmergencyFundService` + `EmergencyFundController` expose the dashboard tile with red/amber/green/insufficient bands. Frontend ships `EmergencyFundCard` above `FireCalculatorCard` and a shared `AccountPicker` mounted in budget + investment transaction dialogs. Backend tests 1145 -> 1207 (+62, exceeds +35 plan target); frontend 236 -> 239 (+3, meets target). `mvnw verify` green; JaCoCo 60% / 45%; Spotless clean. Frontend lint + typecheck + build + Vitest all clean. OpenAPI regen still defers per the pre-existing 26-01 issue; the `BudgetTransaction` / `Bill` contract tests widen the expected keyof set ahead of regen. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md gains `## Linking transactions to accounts` + `### Emergency-fund coverage`.
Original session: 2026-05-07
Stopped at: Phase 27 in progress (2 of 4 plans). Plan 27-02 stood up the standalone `accounts` entity at `/accounts` (Track G2-a): owner-scoped declaration of where value sits across bank checking, bank savings, brokerage cash, crypto wallets, and physical cash, independent of portfolios and transactions in this plan (27-03 wires the FK). New Flyway migration `V41__accounts.sql` (NUMERIC(20,8) balance for satoshi-precision; partial unique index on `(user_id, lower(name)) WHERE is_archived = FALSE` for the live-rows-only duplicate-name guard; composite hot-read index on `(user_id, is_archived)`). New `com.fintrack.common.entity.Account` + nested `AccountType` enum (BANK_CHECKING / BANK_SAVINGS / BROKERAGE_CASH / CRYPTO_WALLET / CASH / OTHER). New `com.fintrack.account` feature package: repository with six query methods, service with list/get/create/update/delete/totalsForUser + 50-account cap + case-insensitive duplicate-name guard + audit success/failure emission mirroring the 24-08 contract, controller with six REST endpoints under `/api/v1/accounts`, four DTO records. Three new AuditAction constants. Frontend ships a lazy-loaded `/accounts` page with sidebar link (Landmark icon between Portfolios and Budget), three stat cards (live count / per-currency balances mini-list / archived count), grouped-by-type list, kebab-menu Edit + Archive flow, and a unified create/edit dialog. New `accounts.*` i18n namespace in both `tr.json` and `en.json`. Backend tests 1111 -> 1145 (+34); frontend 233 -> 236 (+3). `mvnw verify` green; JaCoCo 60% / 45% met; Spotless clean. Frontend lint + tsc strict + Vitest + Vite build all clean. OpenAPI spec regen deferred -- same pre-existing 26-01 OpenTelemetry ComponentLoader issue from 27-01; verified at HEAD; the 23-03 contract gate will catch drift once the regen script is fixed. `.env.example`, `docker-compose.yml`, `CHANGELOG.md`, `package.json`, `package-lock.json`, `backend/pom.xml` left untouched per project guards. OPERATIONS.md gains the `## Managing accounts` H2 with the add/edit/archive workflow + 8-decimal precision note + 27-03 forward reference.
